{
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target node",
    "format": "prettier -w .",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0",
    "@octokit/webhooks-types": "^7.4.0",
    "@types/node": "latest",
    "execa": "^8.0.1",
    "prettier": "^3.0.0",
    "typescript": "latest",
    "bun": "^1.0.33"
  }
}
